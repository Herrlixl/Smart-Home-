<!DOCTYPE html>
<?php
/* if(notloggedin)
	goto index.php 
	
	session?			*/
?>
<head>
<title>Steurung</title>
</head>
<body>
<h1>Steuerung von <?php //echo zimmer ?></h1> <!-- zimmer Variable aus index.php -->
<div>
<?php
/* $db = new PDO(
				"mysql:host=localhost;dbname=test",
				"root",	// USER
				"");	// Passwort von ROOT unter XAMPP

		$sqlBefehl = "SELECT * FROM userdaten ORDER BY vote DESC";			// Where raum = zimmer; zimmer aus index.php

		$daten = $db->prepare( $sqlBefehl ); // Vorbereiten

		if( $daten->execute() )	// lädt 2D-Tabelle in $daten
		{
			$i = 1;
			while( $zeile = $daten->fetch() )	// eine Zeile als Array
			{
				$zbild = $zeile['bild'];
				$zname = $zeile['name'];
			*/ 																// Aufruf Datenbank Beispiel
			
		/*  if(object==button)
			echo  aufruf button + inhat je nach zimmer
			if(object==schieberegler)
			echo  aufruf schieberegler + inhat je nach zimmer
			etc.
			. . .
			}
			dynamische website je nach aufgerufenem zimmer */
?>
</div>
</body>